{
  "name": "adapter-pattern-demo",
  "version": "1.0.0",
  "description": "TypeScript implementation of the Adapter Design Pattern with interactive console demo",
  "main": "dist/demo.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/demo.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
